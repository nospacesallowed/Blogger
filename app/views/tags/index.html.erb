<h1> Akk Tags </h1>

<ul id="tags">
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag.name, tag_path(tag), class: "tag_name" %>
    </li>
    <%end%>
</ul>
<%= link_to "<<  Back to Article list", articles_path %>